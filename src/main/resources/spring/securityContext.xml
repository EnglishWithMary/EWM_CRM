<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true" use-expressions="true">
        <!--<intercept-url pattern="/**" access="permitAll" />-->
        <intercept-url pattern="/admin" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/users**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/users/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/depDel" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/depDelete" access="hasRole('ROLE_ADMIN')"/>
        <form-login/>
        <logout logout-success-url="/home"/>
        <csrf disabled="true"/>
    </http>

    <authentication-manager>

        <authentication-provider>
            <password-encoder hash="bcrypt"/>
            <jdbc-user-service data-source-ref="dataSource" id="userService"
                               users-by-username-query="select login as username, password, true from users where login=?"
                               authorities-by-username-query="select users.login as username, roles.role as authority from users
                               left join roles on users.id=roles.user_id where users.login=?"/>
        </authentication-provider>
    </authentication-manager>
</beans:beans>